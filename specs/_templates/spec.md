# <Module Name>

**Status:** Draft | Approved | In Progress | Done
**Depends on:** <other spec files this one assumes exist, or "none">

## Problem

What user need or gap does this module address? One or two sentences — no implementation detail here.

## Goals

- What this module must do.

## Non-Goals

- What is explicitly out of scope for this version, and why (avoids scope creep and re-litigating decisions already made).

## Data Model

Entities this module owns (Prisma-style shorthand is fine), field-by-field only where the name isn't self-explanatory.

## API Contract

Endpoints this module exposes: method, path, request shape, response shape, auth requirements.

## Behavior Notes

Anything a plain CRUD description wouldn't capture: background jobs, caching, external API calls, edge cases, error handling.

## Acceptance Criteria

- [ ] Testable, concrete statements. Each one should be checkable by running the app or a test, not by reading code.

## Open Questions

- Anything still undecided. Remove this section once empty.
