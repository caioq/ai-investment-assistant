# US-<N>: <short title>

**Status:** Draft | Ready | In Progress | Done
**Traces to:** spec Goal "..." / AC "..." (in `../spec.md`)

As a `<role>`, I want `<goal>`, so that `<benefit>`.

## Tasks

- [ ] [T-1: <short task title>](../tasks/<MODULE>_US-<N>_T-1-<short-task-title>.md)
- [ ] [T-2: <short task title>](../tasks/<MODULE>_US-<N>_T-2-<short-task-title>.md)

<!-- <MODULE> is the module directory name, uppercased with hyphens as underscores (market-data -> MARKET_DATA) — see CONVENTIONS.md -> "Naming". Story IDs (US-<N>) stay bare; only task IDs are module-prefixed. -->


<!-- One line per task file this story owns, in build order. Check the box when that task file's Status is Done — this list is the story's own progress view, the task file is where the detail lives. -->

## Notes

Anything that doesn't fit the one-line story but matters to whoever implements it: edge cases, UX specifics, why this story is worded the way it is. Delete this section if there's nothing to add.
